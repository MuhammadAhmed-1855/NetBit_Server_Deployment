name: Automating NetBit Server Containerization

on:
  push:
   branches: [ AWS ]
  pull_request:
    branches: [ AWS ]
      
jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Build & Push Image
      run: |
        echo "${{ secrets.DOCKERPW }}" | docker login -u "muhammadahmed1855" --password-stdin 
        docker build -t backend .
        docker tag backend muhammadahmed1855/aws_netbit_backend
        docker push muhammadahmed1855/aws_netbit_backend